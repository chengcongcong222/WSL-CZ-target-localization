# R3-RC3-REANCHOR 角色冻结（GPT 审计确认）

UTC: 由生成脚本写入

## 正式冻结

| 层 | 标签 |
| --- | --- |
| 科学实验标签 | **`DEPTH_ROLE_MIXED_OR_UNRESOLVED`** |
| 辅助科学结论 | **`NO_STRONG_DEPTH_COMPENSATION_OBSERVED`** |
| 系统架构角色 | **`DEPTH_PROFILED_NUISANCE_VARIABLE`** |
| 主跟踪状态 | \(x_{\rm track}=[r,\theta,v,\psi]^T\) |
| 深度处理 | \(z\in[150,250]\) 做 \(J_{\rm prof}=\min_z J(h,z)\)（或未来 \(L(h)=\int L(h,z)p(z)dz\)） |
| Liang \(D(z)\) | **暂停**（AR/Hankel 证据作后备，不占主线） |
| Forward gate | `ESTD_RELATIVE_TL_FORWARD_MODEL_VALIDATED`（12/12） |

**不**把实验标签改成 `DEPTH_NUISANCE`（预注册阈值未满足 NONMATERIAL 多数）。

## 含义

完全固定 \(z\) 过乐观；完全忽略 \(z\) 不合适；**对 z 消元后再判水平轨迹**。

后续 RC3：

> 传播信息 → 对 \([r,\theta,v,\psi]\) 候选做 depth-profiled 判别

不再以“单阵精确测深”为主目标。

## 旧 P3 口径

`PROPAGATION_INCREMENT_CONFIRMED / RC2≈0%→97%` 降级为
**`HISTORICAL_P3_SIMPLIFIED_RESULT`**（固定深度/简化条件；非当前 REANCHRC 路线性能，非距离精度）。
